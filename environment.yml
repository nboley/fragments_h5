name: base
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.10
  - setuptools>=61.0
  - numpy
  - h5py
  - pysam
  - tqdm
  - cython
  - pip
  - c-compiler
  - pytest
  - pytest-timeout
  - rattler-build  # For building conda packages

